# 2026-07-27

- 修正退出登录流程：`front-end/js/app.js` 中「我的」页与「设置」页确认退出后，统一调用 `XJAuth.dispatchLogout('auth.html', { skipPrompt: true })`，直接跳转授权入口，不再经过 `onboarding-1.html`。
- 增强 `front-end/js/auth-flow.js` 的 `dispatchLogout`，支持 `skipPrompt`，直达 `auth.html` 时不追加 `?logout=1`；仍清理 `xj_account` / `xj_guest`、保留 `xj_login_history`。
- `app.js` 与 `auth-flow.js` 通过 Node 语法检查；模拟退出验证通过：跳转为 `auth.html`，会话态清空，历史登录记录保留。
- 新增游客模式守卫：`front-end/js/auth-flow.js` 增加 `isGuestMode()`（判断 `xj_guest==='1'` 且 `xj_account!=='1'`）与 `guardGuest(targetPage)`（跳转 `auth.html?from=目标页`）。
- `front-end/js/app.js` 的 `initTabBar()` 接入守卫：游客点击「足迹 / 我的」Tab 时拦截并直接跳转 `auth.html`，已登录账号不受影响。
- 模拟验证：游客点击受限 Tab 跳转 `auth.html?from=footprint.html`；已登录账号 `isGuestMode()` 返回 false；退出仍清会话态并直达 `auth.html`。
- 「我的」/「设置」页退出登录改为项目风格二次确认弹窗：`XJAuth.confirmLogout({ redirectTo: 'auth.html' })`，沿用 `.modal-backdrop` / `.modal-sheet`，含暖橙→淡粉渐变图标 + 「确认退出」(粉红渐变) + 「再想想」(`btn-secondary`)；点确认走 `dispatchLogout('auth.html', {skipPrompt:true})`，清理会话态并跳转授权页，已登录 / 游客身份均触发。
- 游客点「足迹 / 我的」Tab → 跳转授权页 → 切换为强制登录模式：`front-end/auth.html` 新增 `body[data-required-login="true"]` 整体切换（基于 `?from=` 参数），隐藏「以游客身份体验」按钮、把微信登录升级为暖橙主按钮、显示「该功能仅向登录用户开放」提示卡 + 「← 返回上一页」次按钮；目标页名映射 `footprint.html → 温柔足迹`、`me.html → 我的主页`、`release.html → 发布盲盒`、`settings.html → 设置`，否则显示原始 `from`。
- **commit & push**：本批全部前端改动已提交 `main` (`f35aabf`, 14 文件 / +1853 -170)，并推送 `origin/main` (`df768ef..f35aabf`)。`gh-pages` 用 `git archive main front-end/ | tar -x --strip-components=1` 提取源树、剔除 `editor.html / mastergo/ / me-tab-content.md / me-wireframe.svg` 后同步根目录，commit `54b2c14` 推送 `origin/gh-pages` (`02cfb78..54b2c14`)。**注意**：`git checkout gh-pages` 会删除磁盘上 `front-end/` 内 main 跟踪的文件（gh-pages 不跟踪 `front-end/` 子目录），切回 `main` 即恢复，源始终安全。后续同步 gh-pages 推荐复用 `git archive` 法，勿直接依赖磁盘 `front-end/`。

- **修复 #59 游客守卫误伤已登录用户**：`front-end/js/app.js` 的 `initTabBar` 守卫分支原本写的是 `if (... && XJAuth.isGuestMode)` —— 这是函数引用而非调用结果，在 boolean 语境下永远为 truthy，等于让守卫对所有用户生效，连微信登录用户点「足迹 / 我的」也会被踢回 `auth.html`。修正为 `XJAuth.isGuestMode()` 真正调用判断；同时把硬编码 `guardGuest('footprint.html')` 改为 `guardGuest(href)`，让 `?from=` 反映实际点击的 tab（点「我的」时为 `me.html`）。Node 桩测试 5 个状态 + 微信登录不被拦截均通过。本地预览 `http://localhost:8080/` 实时生效，等待 commit/push。

- **#59 已上线 #60**：`main` 提交 `40600d4`、`gh-pages` 同步 `0a359fd`，均已 push 到 origin，Pages CDN 已落地验证 `isGuestMode()` + `guardGuest(href)` 两处修复生效。**网络教训**：本机 `env HTTPS_PROXY=127.0.0.1:53297` 偶发对 `github.com`（不含 `api.github.com`）长时间 502 / `CONNECT tunnel failed`；git push 直接走 `github.com`，需用 `git -c http.version=HTTP/1.1 push` + 等待代理恢复。症状是 `curl https://github.com` 持续超时但 `curl https://api.github.com` <1s 返回 —— 可作为快速甄别手段。
