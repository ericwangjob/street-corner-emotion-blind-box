# 2026-07-22 工作日志

## 深色模式（Dark Mode）完成并上线
- 在现有 H5 原型上实现完整深色主题：
  - `css/design-system.css` 新增 `html.dark` token + 组件覆盖（背景/文字/边框/强调色，全部 `!important` 以压过 themes.js 内联的时段主题变量）。
  - 新增 `js/theme-toggle.js`：三态切换（浅色/深色/跟随系统），localStorage 持久化 `xj_theme_mode`，同步 `meta theme-color` 与派发 `xj:themechange` 事件。
  - 全部 6 个页面加入无闪烁 bootstrap（`<head>` 内联脚本，绘制前加 `.dark`）+ 引入 theme-toggle.js。
  - `js/app.js` 画布图表/导出按 `html.dark` 分支配色，并监听 `xj:themechange` 重绘。
  - 平滑过渡：`html.theme-anim` 门控（首次 rAF 后启用），遵循 `prefers-reduced-motion`。
- 已提交 `main` (9e8f63f) 并同步 `gh-pages`，线上演示已生效。

## gh-pages 源清理（隐私 + 整洁）
- 之前同步误将 dev/tooling 产物提交进 gh-pages 根目录：`.workbuddy/`（含项目记忆，已公开 → 隐私风险）、`.cache/`、`.cloudstudio`、`ardot/`、`frontend/`（旧 Vite 脚手架）、`front-end/`、`Remove_the_white_circular_play*.png`，以及 `assets/` 下两个 `.DS_Store`。
- 用独立 git worktree（/tmp）安全 `git rm --cached -r` 移除，磁盘文件不动；gh-pages 新增 `.gitignore` 防止复发。
- 推送后验证：站点根 200；原 `.workbuddy/memory/MEMORY.md` 已返回 404（泄露修复）；stray 扫描 NONE — clean。
- gh-pages 当前根目录仅含 `css/ js/ assets/ *.html README.md .gitignore`。

## release.html 提交按钮 disabled 门控逻辑
- 按用户明确要求「初始状态为未选」，撤销原默认选中的 `relaxed` 情绪：去掉 `.mood-option.selected` 类与 `aria-checked="true"`，隐藏域 `<input name="mood" value="">` 保持空。
- 提交按钮加 `id="btn-submit"` + `disabled`；`css/design-system.css` 新增 `.btn:disabled` 视觉态（opacity .45 / 灰阶 / 禁指针）。
- `js/app.js` 新增 `validateRelease()`（模块级），三重 AND 门控：**(a) 已点击拍照或录音（#media-capture[data-state]≠empty）**(b) 已选情绪色（mood 隐藏域非空）**(c) 文字框有非空白输入**。三者全满足才可点。
- 触发点：`setState()`（媒体增/删自动重算）、`initMoodSelector` 选色、`initReleaseForm` 文字 input + 加载时初算；`releaseLocked` 标志在提交流程中锁死，避免被重算重新启用。
- node --check 通过。待用户确认后提交 push。
